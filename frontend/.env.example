# Dev proxy mode: frontend calls /api/*, Vite forwards to VITE_PROXY_TARGET.
VITE_PROXY_TARGET=http://localhost:8080

# Production mode: frontend calls API by absolute URL (separate API domain).
VITE_API_BASE_URL=https://api.example.com
